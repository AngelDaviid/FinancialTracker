query GetAccount($id: ID!) {
    account(id: $id) {
        id
        name
        type
        currency
        balance
        color
        icon
        creditLimit
        currentDebt
        availableCredit
        cutoffDay
        paymentDueDay
        interestRate
        minimumPayment
        createdAt
    }
}