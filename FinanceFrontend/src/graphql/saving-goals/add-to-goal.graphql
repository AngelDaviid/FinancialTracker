mutation AddToSavingsGoal($input: AddToGoalInput!) {
    addToSavingsGoal(input: $input) {
        id
        name
        currentAmount
        progress
        remaining
        isCompleted
        linkedAccount {
            id
            name
            balance
        }
    }
}